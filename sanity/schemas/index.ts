import { productSchema } from "./product";
import { testimonialSchema } from "./testimonial";
import { salesPointSchema } from "./salesPoint";

export const schemaTypes = [productSchema, testimonialSchema, salesPointSchema];
